server:
  port: 9004

spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #??Nacos??

management:
  endpoints:
    web:
      exposure:
        include: '*'






